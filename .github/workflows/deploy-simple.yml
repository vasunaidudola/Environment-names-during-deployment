run-name: Deploy ${{ inputs.tag }} → ${{ inputs.aws_region }}

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Git tag
        required: true
      aws_region:
        description: AWS Region
        required: true
        type: choice
        options:
          - us-east-1
          - ap-south-1
